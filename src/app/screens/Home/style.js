import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconContainer: {
        width: 30,
        height: 30,
        justifyContent: 'center',
        alignItems: 'center',
        marginLeft: 5
    },
    progressContainer: {
        height: 40,
        width: '100%',
        alignItems: 'center',
        justifyContent: 'center'
    },
    progress: {
        width: 30, 
        height: 30
    }
});