import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    textContainer: {
        paddingVertical: 5,
        textTransform: 'capitalize',
        fontWeight: 'bold',
        color: '#424242',
        fontSize: 15
    },
    stat: {
        fontWeight: 'normal'
    }
});